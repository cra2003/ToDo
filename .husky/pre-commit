#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🔍 Running Prettier (auto-format)..."
npx prettier --write . --log-level warn
PRETTIER_RESULT=$?

if [ $PRETTIER_RESULT -ne 0 ]; then
  echo "⚠️  Prettier encountered issues, but continuing..."
fi

echo "🔍 Running ESLint..."

npm run lint

RESULT=$?

if [ $RESULT -ne 0 ]; then
  echo "❌ ESLint errors detected. Commit blocked."
  exit 1
fi

echo "✅ ESLint passed. Proceeding with commit."
